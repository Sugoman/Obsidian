[[HTML — Структура 💻]]

# Изображение

Тег `img` является *одиночным тегом* (подробнее: [[Синтаксис]]) и имеет один обязательный атрибут `src=""`. 
В значение атрибута указывается путь до файла изображения любого формата который поддерживает браузер. Помимо `src` хорошим тоном является задавать ещё один атрибут `alt`, в значение этого атрибута прописывается альтернативное небольшое описание изображения. Этот текст пользователь в обычной ситуации не увидит. Если картинка по какой-либо ситуации не загрузится, то вместо изображения браузер отобразит альтернативный вариант - текст внутри `alt`. Также `alt` полезен для людей с особенным восприятием визуальной информации, скринридеры читают именно `alt`. 
Важно, `alt` следует заполнять, если картинка имеет ценность для пользователя, то есть если изображение сугубо декоративное, то значение `alt` стоит оставлять пустым, но НЕ добавлять `alt` нельзя не при каких обстоятельствах!
Так же хорошей практикой будет добавлять в тег `img` атрибуты `width` - ширина и `height` - высота. В значение указываются числа без единиц измерения.

Дополнительные теги для `<img>`
- `title` - всплывающая подсказка при наведении. Хорошо дополняет `alt`.
- `loading="lazy"` - отложенная загрузка. Картинка будет загружена только при прокрутке до неё, помогает ускорить загрузку страницы.
- `decoding="async"` - позволяет декодировать изображения асинхронно, ускоряя отрисовку. 
- `srcset` и `sizes` - для адаптивных изображений. Позволяет грузить картинку нужного разрешения в зависимости от экрана. 

**Пример:**
```html
<img src="default.jpg" 
     srcset="img-small.jpg 480w, img-big.jpg 1080w" 
     sizes="(max-width: 600px) 480px, 1080px"
     alt="Описание">
```

---

# Видео

Тег `<video>` используется для встраивания видеофайлов на страницу. Тег **не одиночный**.

**Пример:**
```html
<video src="movie.mp4" controls></video>
```

**Атрибуты:**
- `src` - путь до видеофайла.
- `controls` - добавляет стандартные элементы управления.
- `autoplay` - запускает видео автоматически. (Часто блокируется браузером)
- `muted` - отключает звук.
- `loop` - зацикливает видео.
- `playsinline` - меняет стандартное поведение видеоплеера на мобильных устройствах при загрузке страницы. Вместо того чтобы видео открывалось стразу в полноэкранном режиме, оно будет открываться в стандартных рамках своего элемента
- `poster=""` - отображает обложку до начала воспроизведения. 
- `preload="auto|metadate|none` - управляет предварительной загрузкой
	- `auto` - загрузить всё сразу
	- `metadata` - только информацию (длительность и т.д.)
	- `none` - не грузить до запуска

**Пример:**
```html
<video controls width="640" height="360" poster="preview.jpg">
  <source src="video.mp4" type="video/mp4" />
  <source src="video.webm" type="video/webm" />
  <p>К сожелению, ваш браузер не поддерживает видео. </p>
</video>
```

#### Примечания:

- Для полной кроссбраузерности желательно использовать несколько форматов (например `.mp4`, `.webm`).
- Если видео — просто декор, лучше использовать `muted autoplay loop` и скрыть управление (`controls` не указывать).

--- 

Тег `<audio>` выводит на страницу компактный аудиоплеер от браузера, атрибуты применяются практически такие же как и для `<video>`

```html
<audio controls>
	<source src="./audio/example.mp3" type="audio/mpeg">
	<source src="./audio/example.ogg" type="audio/ogg">
	<p>
	Ваш браузер не поддерживает встроенное аудио. Попробуйте <a href="./audio/example.ogg" download>скачать</a> файл.
	</p>
</audio>
```

---

Тег `<iframe>` позволяет вывести на страницу виджет с каким-то функционалом, например: виджет погоды, интерактивную карту, видеоплеер YouTube и т.д., а так же тег `<iframe>` может вывести другой сайт.

**Основные атрибуты тега:**
- `src=""` - источник фрейма.
- `width` и `height` - указатели размера фрейма.
- `frameborder="1"` - указывает на наличие или отсутствие рамки (атрибут устаревший, добавлять не стоит)
- `title` - текст для скринридеров 
- `allow` - указывает политику различных разрешений, определяет функции, доступные для фрейма
- `allowfullscreen` - задаёт разрешение запускаться фрейму в полноэкранном режиме 

**Пример от Яндекс:**
```html
<iframe src="https://yandex.ru/map-widget/v1/?um=constructor%3A1577ad6cf73e0f082aa91b0559accbc301ec993a3e0161e482c9e827df44d840&amp;source=constructor"
    width="500" 
    height="400"
    frameborder="0">
</iframe>
```

**Пример YouTube:**
```html
<iframe 
width="560"
height="315" 
src="https://www.youtube.com/embed/6PL2TqBdz0I?si=Wh9jsLY2yW3-36XS" title="YouTube video player" 
frameborder="0" 
allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
referrerpolicy="strict-origin-when-cross-origin" 
allowfullscreen>
</iframe>
```